import { createAction } from '@ngrx/store';

export const startProgressbars = createAction('[Progressbar] Start Progressbars');
export const CompleteProgressbars = createAction('[Progressbar] Complete Progressbars');
